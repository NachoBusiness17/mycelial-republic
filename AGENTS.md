# mycelial-republic — project rules

## Operator context

See also global `~/.grok/AGENTS.md` (Quixote / sovereign mirror).

This repo is the **product path**: data → scaffold → optional train → spore.  
Instrument/EUT lab lives in `../worktrees/sovereign-mirror-scaffold` when needed.

**Zeitgeist:** many free minds, each with their own residual “beads”; Mag (`../local_sovereign_agent`) is the person-scale office; this repo is the **forest/spore** path. See `docs/ZEITGEIST_BEADS.md`. Never ship private residual; no core-mirror privilege.

## Current phase (honest)

- **Now:** craft + Locus + `/strike-chord` + curated high-fid corpus + iterate  
- **Not yet:** full archive gate as blocker, local multi-GPU lab, GSTD dependence  
- **Train:** optional later (cloud/GSTD/lab when he chooses); never the definition of progress  

## Agent behavior here

1. Prefer unblocking **useful mirror practice** over foyer features.  
2. Ingest paths: `data/bookmarks/`, `thread-pull`, annotate — private data never committed.  
3. Do not mark R0 complete without evidence in `docs/MILESTONES.md`.  
4. Saelis/Steiniger scaffolds = **structure templates**, not operator identity to ship.  
5. One job per turn. No framework recitation as filler.  
6. **Scrum:** start sessions with `mycelia scrum status`; pull one ticket; standup; done with evidence. See `docs/SCRUM.md`.

## Python env

Shell `python` is often **Hermes** (wrong site-packages). Use project tools:

```text
mycelia.cmd --help
mycelia.cmd scaffolds
mycelia.cmd vector-map
.\.venv\Scripts\python.exe -m mycelial_republic.cli <cmd>
```

Repair: `powershell -ExecutionPolicy Bypass -File .\scripts\bootstrap.ps1`

## Key paths

| Path | Role |
|------|------|
| `docs/CONSTITUTION.md` | Binding Lessig constitution (W0.4) |
| `docs/SCRUM.md` | Agent/PO scrum process |
| `scrum/` | Backlog, current sprint board, standups |
| `CONTRIBUTING.md` | Contributor entry |
| `scaffolds/system_prompt_v2.txt` | Hybrid Locus |
| `docs/AGENT_ROADMAP.md` | Long DAG (use when building) |
| `data/annotated/mirror_train.jsonl` | Current seed corpus |
| `../local_sovereign_agent/` | LangGraph + Ollama instrument (not product data) |
| `~/.grok/skills/strike-chord/` | Full chord mode |
| `~/.grok/AGENTS.md` | Global operator rules |
